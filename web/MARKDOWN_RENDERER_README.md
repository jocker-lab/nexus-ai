# 📊 Nexus AI 炫酷报告渲染系统

一个美观、现代化的Markdown报告渲染系统，具有丰富的视觉效果和强大的功能。

## ✨ 功能特性

### 🎨 视觉效果
- **渐变背景动画** - 动态紫-蓝-青渐变背景
- **玻璃态设计** - 半透明卡片+模糊背景效果
- **平滑动画** - Framer Motion驱动的流畅过渡
- **深色/浅色主题** - 一键切换，自动保存偏好

### 📝 Markdown增强
- ✅ **GFM支持** - 表格、删除线、任务列表
- ✅ **数学公式渲染** - KaTeX高性能数学公式
- ✅ **流程图支持** - Mermaid图表（流程图、时序图、甘特图等）
- ✅ **代码高亮** - 精美语法高亮+一键复制
- ✅ **图片优化** - 懒加载+悬浮放大效果

### 🧭 阅读体验
- **智能目录导航** - 自动生成TOC，当前章节高亮
- **阅读进度追踪** - 顶部进度条+剩余时间预估
- **平滑滚动** - 点击目录平滑跳转

### 📄 导出功能
- **PDF导出** - 保留所有样式的高质量PDF
- **复制内容** - 一键复制纯文本

## 🚀 快速开始

### 1. 启动后端（如果还没运行）

```bash
# 在项目根目录
python main.py
# 或
uvicorn main:app --reload --port 8000
```

### 2. 启动前端

```bash
cd web
npm run dev
```

### 3. 访问报告

打开浏览器访问：
```
http://localhost:3000/reports/[报告ID]
```

例如：
```
http://localhost:3000/reports/abc123-def456-ghi789
```

## 📁 项目结构

```
web/app/reports/
├── [id]/
│   └── page.tsx              # 报告详情页面
├── components/
│   ├── MarkdownRenderer.tsx  # Markdown渲染器
│   ├── CodeBlock.tsx         # 代码高亮组件
│   ├── MermaidBlock.tsx      # Mermaid图表
│   ├── MathBlock.tsx         # 数学公式
│   ├── TableOfContents.tsx   # 目录导航
│   ├── ReadingProgress.tsx   # 阅读进度
│   ├── ReportHeader.tsx      # 报告头部
│   ├── ThemeToggle.tsx       # 主题切换
│   └── ExportButton.tsx      # 导出按钮
├── hooks/
│   └── useReportData.ts      # 数据获取Hook
├── styles/
│   ├── markdown.css          # Markdown样式
│   ├── glassmorphism.css     # 玻璃态效果
│   └── animations.css        # 动画效果
└── utils/
    └── pdf-export.ts         # PDF导出工具
```

## 🎯 Markdown功能示例

### 代码块（带语法高亮）

\`\`\`python
def hello_world():
    print("Hello, World!")
\`\`\`

### 数学公式

行内公式：`$E = mc^2$`

块级公式：
```
$$
\\int_0^\\infty e^{-x^2} dx = \\frac{\\sqrt{\\pi}}{2}
$$
```

### Mermaid流程图

\`\`\`mermaid
graph TD
    A[开始] --> B{条件判断}
    B -->|是| C[执行A]
    B -->|否| D[执行B]
    C --> E[结束]
    D --> E
\`\`\`

### 表格

| 特性 | 状态 | 备注 |
|------|------|------|
| 代码高亮 | ✅ | 支持150+语言 |
| 数学公式 | ✅ | KaTeX渲染 |
| 流程图 | ✅ | Mermaid支持 |

### 任务列表

- [x] 完成Markdown渲染器
- [x] 集成代码高亮
- [ ] 添加更多主题

## ⚙️ 配置

### 环境变量

编辑 `web/.env.local`：

```env
# 后端API地址
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000
```

### 主题颜色

在 `web/app/reports/styles/` 文件中修改CSS变量即可自定义颜色方案。

## 🎨 自定义样式

### 修改渐变背景

编辑 `web/app/reports/styles/animations.css`:

```css
.animated-gradient-bg {
  background: linear-gradient(
    -45deg,
    #你的颜色1,
    #你的颜色2,
    #你的颜色3
  );
}
```

### 修改玻璃态透明度

编辑 `web/app/reports/styles/glassmorphism.css`:

```css
.glass-content {
  background: rgba(255, 255, 255, 0.7); /* 调整透明度 */
  backdrop-filter: blur(30px); /* 调整模糊程度 */
}
```

## 🔧 API集成

### 后端要求

确保后端API返回以下格式的报告数据：

```json
{
  "id": "string",
  "title": "string",
  "description": "string",
  "content": "markdown字符串",
  "category": "string",
  "tags": "tag1,tag2,tag3",
  "word_count": 5000,
  "estimated_reading_time": 15,
  "status": "published",
  "created_at": "2024-01-01T00:00:00",
  "updated_at": "2024-01-01T00:00:00",
  "current_version": 1
}
```

### 添加新的API端点（可选）

如果需要其他端点，在 `web/app/api/` 下创建新的路由文件。

## 📱 响应式设计

系统完全响应式，支持：
- 桌面端：完整侧边栏导航
- 平板：自适应布局
- 移动端：悬浮按钮式导航

## 🐛 常见问题

### Q: 报告加载失败？
A: 确保后端已启动且 `.env.local` 中的 `NEXT_PUBLIC_BACKEND_URL` 配置正确。

### Q: 数学公式不显示？
A: 使用 `$...$` 或 `$$...$$` 语法，确保LaTeX语法正确。

### Q: Mermaid图表不渲染？
A: 确保使用 \`\`\`mermaid 代码块，语法参考 [Mermaid文档](https://mermaid.js.org/)。

### Q: PDF导出失败？
A: 检查浏览器控制台错误，可能是权限或Canvas相关问题。

## 🚢 生产部署

### 构建生产版本

```bash
cd web
npm run build
npm start
```

### Docker部署（可选）

创建 `Dockerfile`:

```dockerfile
FROM node:18-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci --only=production
COPY . .
RUN npm run build
EXPOSE 3000
CMD ["npm", "start"]
```

构建并运行：

```bash
docker build -t nexus-ai-web .
docker run -p 3000:3000 -e NEXT_PUBLIC_BACKEND_URL=http://your-backend:8000 nexus-ai-web
```

## 📈 性能优化

已实现的优化：
- ✅ 代码分割
- ✅ 图片懒加载
- ✅ CSS模块化
- ✅ 组件按需加载

建议的进一步优化：
- [ ] 添加服务端渲染（SSR）
- [ ] 实现报告缓存
- [ ] CDN加速静态资源

## 🤝 贡献

欢迎贡献！可以：
1. 添加更多Mermaid图表类型支持
2. 增强PDF导出功能
3. 添加更多代码高亮主题
4. 优化移动端体验

## 📄 许可

本项目遵循 Nexus AI 项目的许可协议。

---

**Enjoy your beautiful reports! 🎉**
